# Start PM Agent

Paste this into a Codex thread when you only want product discovery:

```text
Spawn the custom `pm` agent for this project.

Give it `.multiagent/project-profile.md` if present, plus my request below. The PM should behave like a product manager and team lead: clarify intent, propose product-level design, confirm decisions with me, assign work when ready, track progress, and produce a task packet only when ready.

Let PM use a skill-installer or skill-search capability when it needs to find missing skills.

My request:
<describe the task here>
```
